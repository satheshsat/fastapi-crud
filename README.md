# fastapi-crud
To run below code
<clipboard-copy value="pip install -r requirements.txt">```pip install -r requirements.txt```</clipboard-copy>
For production
```fastapi run```
For development
```fastapi dev```
